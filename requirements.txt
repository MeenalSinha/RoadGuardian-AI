# ============================================================================
# Road Pothole Detection - Python Dependencies (Streamlit Cloud Optimized)
# ============================================================================
# Minimal dependencies for deployment with version constraints

# Core ML/CV
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Visualization (with version constraints to ensure compatibility)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0
pydeck>=0.8.0
altair>=5.0.0,<6.0.0  # Pin Altair to v5.x for Streamlit compatibility

# Augmentation
albumentations>=1.3.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
requests>=2.31.0

# Streamlit (pinned to compatible version)
streamlit>=1.28.0,<2.0.0

# Additional dependencies that might be needed
protobuf>=3.20.0,<5.0.0  # Prevents protobuf version conflicts
